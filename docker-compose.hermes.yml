services:
  hermes:
    image: nousresearch/hermes-agent:v2026.9.14
    container_name: jarvis-hermes
    restart: unless-stopped
    command: ["gateway", "run"]
    env_file:
      - path: /root/jarvis/config/hermes.env
        required: false
      - path: /root/jarvis/.env
        required: false
      - path: .env
        required: false
    environment:
      API_SERVER_ENABLED: "true"
      API_SERVER_HOST: "0.0.0.0"
      API_SERVER_PORT: "8642"
      TZ: Africa/Johannesburg
      GITHUB_TOKEN: ${GITHUB_TOKEN:-${GITHUB_PAT:-}}
      GITHUB_PAT: ${GITHUB_PAT:-${GITHUB_TOKEN:-}}
      VERCEL_TOKEN: ${VERCEL_TOKEN:-}
      VERCEL_API_TOKEN: ${VERCEL_TOKEN:-}
    expose:
      - "8642"
    volumes:
      - jarvis-hermes-data:/opt/data
      - ./:/opt/data/workspace
      - ./hermes/config.yaml:/opt/data/config.yaml:ro
      - /root/jarvis/config/api_keys.json:/opt/data/api_keys.json:ro
    networks:
      - default
      - dokploy-network

volumes:
  jarvis-hermes-data:
    name: jarvis-hermes-data

networks:
  default:
  dokploy-network:
    external: true
